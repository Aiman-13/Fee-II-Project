# rvite-app (React + Vite - JSX)

Quick scaffolded app using Vite with React in JSX format.

Commands (after dependencies installed):

```powershell
cd D:\\nfilm\\rvite-app
npm run dev
```

Files created:
- `index.html`
- `vite.config.js`
- `src/main.jsx`
- `src/App.jsx`
- `src/index.css`

Next steps: initialize npm and install dependencies (react, react-dom, vite, @vitejs/plugin-react).
